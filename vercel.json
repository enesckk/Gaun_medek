{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "frontend/$1"
    }
  ],
      "installCommand": "npm install && cp -r backend api/backend && rm -rf api/backend/node_modules && ln -s ../../node_modules api/backend/node_modules || cp -r node_modules api/backend/node_modules && cd frontend && npm install"
}

